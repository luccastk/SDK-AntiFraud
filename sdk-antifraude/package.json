{
  "name": "sdk-antifraud",
  "version": "1.0.0",
  "description": "SDK AntiFraud - Coleta fingerprints do frontend, analisa no backend e retorna decisões de risco",
  "main": "dist/index.js",
  "type": "module",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "prepublish": "npm run clean && npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/luccastk/SDK-AntiFraud.git"
  },
  "keywords": ["antifraud", "security", "fingerprint", "fraud-detection", "sdk", "typescript", "nodejs"],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/luccastk/SDK-AntiFraud/issues"
  },
  "homepage": "https://github.com/luccastk/SDK-AntiFraud#readme",
  "devDependencies": {
    "@types/express": "^5.0.2",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "axios": "^1.9.0",
    "express": "^5.1.0",
    "undici-types": "^7.10.0"
  }
}
